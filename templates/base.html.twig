<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>{% block title %}Le bon coin coin{% endblock %}</title>
    
  </head>
  <header>
  {% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark d-flex fixed-top">
      <div class="mr-auto p-2">
        <a href="/" class="text-decoration-none text-light navbar-brand">Le bon coin coin</a>
        {% if app.user %}
          <a href="/add" class="text-decoration-none text-secondary navbar-brand nav-link">Déposer une annonce</a>
        {% endif %}
      </div>
      <div class="p-2">
      {% if app.user == null %}
        <a href="/login" class="text-decoration-none text-secondary navbar-brand nav-link">Se connecter</a>
        <a href="/register" class="text-decoration-none text-secondary navbar-brand nav-link">S'inscrire</a>
        {% endif %}
      {% if app.user %}
        <span class="navbar-brand">Bonjour {{app.user.email}}</span>
        <a href="{{ path('app_logout') }}" type="button" class="btn btn-danger">Logout</a>
      {% endif %}
      </div>
    </nav>
  {% endblock %}
  </header>
  <body>
   {% block body %}
   {% endblock %}
  </body>

  <footer>
  {% block footer %} 
  <div class="bg-dark text-light card-footer d-flex justify-content-center">
    <h3>Projet fictif réalisé par Arthur CLOUX à destination de Mme Maeva DESSART</h3>
  </div>
  {% endblock %}
  </footer>


  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      padding-bottom: 85px;
      padding-top: 85px;
    }

  </style>
</html>